'use strict';

angular.module('contactsList')
    .component('contactsList', {
        templateUrl: 'scripts/contacts-list/contacts-list.template.html',
        controller: 'ContactsListController'
    });
